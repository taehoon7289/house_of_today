import 'package:flutter/material.dart';

class MyHomeExpertQna extends StatelessWidget {
  const MyHomeExpertQna({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('질문과답변'),
      ),
    );
  }
}
