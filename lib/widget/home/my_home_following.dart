import 'package:flutter/material.dart';

class MyHomeFollowing extends StatelessWidget {
  const MyHomeFollowing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('팔로잉'),
      ),
    );
  }
}
