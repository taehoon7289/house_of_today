import 'package:flutter/material.dart';

class MyHomePicture extends StatelessWidget {
  const MyHomePicture({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('사진'),
      ),
    );
  }
}
